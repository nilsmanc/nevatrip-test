export type Data = {
  timeCases: {
    to: string[]
    back: string[]
  }
  destinations: {
    to: string
    back: string
    round: string
  }
  prices: {
    to: number
    back: number
    round: number
  }
}
